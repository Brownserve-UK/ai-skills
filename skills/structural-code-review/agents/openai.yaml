interface:
  display_name: Structural Code Review
  short_description: Strict review of design, structure, and defects in a code change
  default_prompt: Use $structural-code-review to examine the current branch against its merge base.
policy:
  products:
    - codex
    - chatgpt
  # The review is strict and slow. The user starts it on purpose.
  allow_implicit_invocation: false
